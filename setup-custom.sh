# setup git info
# git config --global user.name "zsney"
# git config --global user.email "zsney.lzhou@gmail.com"
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status

# Update hostname
# sudo scutil --set ComputerName "lzhou"
# sudo scutil --set HostName "lzhou"
# sudo scutil --set LocalHostName "lzhou"
# sudo defaults write /Library/Preferences/SystemConfiguration/com.apple.smb.server NetBIOSName -string "lzhou"
